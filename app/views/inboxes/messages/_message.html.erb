<div id="<%= dom_id message %>" class="border">
  <p>
    <strong>Body:</strong>
    <%= simple_format(message.body) %>
  </p>

  <p>
    <strong>Inbox:</strong>
    <%= link_to message.inbox.name, message.inbox %>
  </p>

  <p>
    <strong>User:</strong>
    <%= message.user.email %>
  </p>

  <%= button_to 'Like', [:upvote, @inbox, message], method: :patch, data: { turbo: "false" }, class: "btn btn-primary btn-xs" %>

<%= message.cached_votes_total%>
<%= message.cached_votes_score%>
<%= message.cached_votes_up%>
<%= message.cached_votes_down%>
<%= message.cached_weighted_score%>
<%= message.cached_weighted_total%>
<%= message.cached_weighted_average%>

  <%= button_to "Destroy", inbox_message_path(@inbox, message), method: :delete, class: "btn btn-primary btn-xs" %>

</div>
